# 金融保单敏感信息扫描工具 — 设计文档

> 日期：2026-05-29  
> 状态：设计稿（待审批）

---

## 一、项目背景

金融保险行业在客户信息管理中有大量保单文件。保单首页包含被保人身份、险种、金额等敏感信息，后续合同页为通用模板，不属于关注范围。传统扫描工具基于规则提取姓名、手机号、地址等字段，虽然产生大量敏感标签，但缺乏业务价值。本工具的目标是**理解保单内容**，提取真正有意义的业务字段，并基于**个体（被保人）** 真实统计敏感信息条数。

---

## 二、整体流程

```
用户上传（PDF/图片）
      ↓
 [文件预处理]
   - PDF → 每页转图片（PyMuPDF）
   - 图片尺寸统一，提高OCR质量
      ↓
 [OCR识别]  ← PaddleOCR（离线，本地CPU运行）
   - 提取所有文字块 + 位置坐标
      ↓
 [页面分类]
   - 判断当前页是否为保单首页
   - 非首页自动跳过
      ↓
 [字段提取]  ← 规则 + 关键词匹配
   - 保险公司 | 险种 | 保单号 | 投保人 | 被保人
   - 受益人 | 保费 | 交费方式 | 生效日期 | 保险期间 | 销售经理
      ↓
 [敏感信息统计]
   - 按被保人姓名去重统计
   - 1个被保人 = 1条有效敏感信息
   - 同一人有多个保单仍算1条
      ↓
 [结果展示]
   - Web界面：表格列表 + 编辑修正 + 统计汇总
   - 导出：Excel / JSON
```

---

## 三、功能模块设计

### 3.1 UploadHandler（上传处理）

| 项目 | 说明 |
|------|------|
| 输入 | 用户上传的文件 |
| 校验 | 文件格式：PDF、JPG、PNG |
| 输出 | 文件保存至临时目录，返回文件列表 |
| 批量 | 支持一次选择多个文件 |

### 3.2 PdfProcessor（PDF转图片）

| 项目 | 说明 |
|------|------|
| 依赖 | PyMuPDF（fitz） |
| 功能 | 将PDF每页转为JPEG图片 |
| 配置 | 输出分辨率 200 DPI，平衡速度与清晰度 |
| 输出 | 图片文件列表（每页1张） |

### 3.3 OcrEngine（OCR识别）

| 项目 | 说明 |
|------|------|
| 引擎 | PaddleOCR（paddlepaddle-cpu 模式） |
| 离线 | 完全离线运行，预下载中文模型 |
| 输入 | 图片文件 |
| 输出 | 文字块列表 `[{text, confidence, bbox}]` |

### 3.4 PageClassifier（页面分类器）

| 项目 | 说明 |
|------|------|
| 功能 | 判断当前页是否为保单首页 |
| 策略 | 关键词匹配（如"保险单"、"保单号"、"投保人"等高频词出现N个以上则判定为首页） |
| 输出 | `True` / `False` |
| 非首页 | 跳过，不影响其他文件 |

### 3.5 FieldExtractor（字段提取器）

| 字段 | 提取策略 |
|------|---------|
| 保险公司 | 常见保险公司名称列表匹配（中国人寿、平安、太平洋、泰康、新华等） |
| 险种 | 关键词"险种"、"产品名称"等后邻文字段提取 |
| 保单号 | 关键字"保单号"后邻文字，或特定编号格式匹配 |
| 投保人 | 关键字"投保人"后邻文字 |
| 被保人 | 关键字"被保险人"、"被保人"后邻文字 |
| 受益人 | 关键字"受益人"后邻文字（可选字段） |
| 保费 | 关键字"保费"、"保险费"后邻数字+单位 |
| 交费方式 | 关键字"交费方式"、"缴费方式"后邻文字 |
| 生效日期 | 关键字"生效日期"、"生效日"后邻日期格式 |
| 保险期间 | 关键字"保险期间"、"保险期限"后邻文字 |
| 销售经理 | 关键字"销售经理"、"业务员"、"代理人"后邻文字 |

提取策略说明：
- 定位关键词 → 根据坐标或文字顺序取邻接字段值
- 支持表格布局（行/列定位）和段落布局
- 置信度过滤：OCR置信度低于 0.7 的字段标记为"待确认"

### 3.6 StatisticsEngine（统计引擎）

| 项目 | 说明 |
|------|------|
| 统计口径 | **按被保人姓名去重** |
| 统计逻辑 | 收集所有被保人姓名 → 去重 → 计数 |
| 示例 | 3个保单，2个不同被保人 = 2条敏感信息 |
| 输出 | 敏感信息总条数 + 详细列表 |

### 3.7 ReportViewer（结果展示）

- **技术**：原生 HTML/CSS/JS（无前端框架）
- **功能**：
  - 上传区域（拖拽/点击）
  - 识别进度提示
  - 结果表格展示（所有字段列）
  - 单元格双击可编辑修正
  - 敏感信息统计汇总
  - 导出 Excel（openpyxl）
  - 导出 JSON

---

## 四、技术选型

| 层面 | 技术 | 理由 |
|------|------|------|
| 后端语言 | Python 3.11+ | OCR生态好，PaddleOCR原生支持，字段提取逻辑灵活 |
| Web框架 | FastAPI | 轻量、异步、自动生成API文档 |
| OCR引擎 | PaddleOCR (paddlepaddle-cpu) | 离线下中文识别率最高，CPU即可运行 |
| PDF处理 | PyMuPDF (fitz) | 快速将PDF页转图片 |
| 前端 | 原生HTML/CSS/JS | 界面简单，无需前端工程化 |
| 导出 | openpyxl (Excel) | 结构化导出Excel报表 |
| 部署 | `python main.py` 启动，浏览器访问 | 最简单直接 |

> 注：不需要数据库，结果暂存内存，刷新即清空。不包含用户系统。

---

## 五、异常处理

| 场景 | 处理方式 |
|------|---------|
| 文件格式不支持 | 返回提示，跳过 |
| OCR置信度低于阈值 | 标记"待确认"，人工可修正 |
| 非保单页 | 标记"无法识别"，不影响其他文件 |
| PDF加密/损坏 | 返回错误提示 |
| 批量中部分失败 | 成功的正常展示，失败的单独标注 |
| 重复文件 | 简单覆盖（不做自动去重） |

---

## 六、非功能性需求

| 指标 | 目标 |
|------|------|
| 离线运行 | 完全离线，不需要联网 |
| 中文识别 | PaddleOCR中文模型，准确率 > 90%（常规印刷保单） |
| 启动时间 | 首次加载模型约 5-15 秒 |
| 每页识别速度 | 约 1-5 秒（取决于图片大小和复杂度） |
| 单机运行 | 仅限本机使用，不支持多用户并发 |

---

## 七、后续可能的扩展（本次不实现）

- 批量文件夹扫描
- 历史记录持久化
- 多用户/权限
- 自定义字段模板
- 打包为独立exe
